import { LOADING, ERROR, GET_USER_STATE } from '../types/userTypes';

export const putPreferences = () => async (dispatch) => {
  return 'putPreferences';
};

export const putProcess = () => async (dispatch) => {
  return 'putPreferences';
};

export const putFinalized = () => async (dispatch) => {
  return 'putPreferences';
};
